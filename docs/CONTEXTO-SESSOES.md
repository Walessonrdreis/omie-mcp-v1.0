# Contexto das Sessões

Log cronológico de cada sessão de trabalho relevante neste projeto: o que foi feito, decisões tomadas e o que ficou pendente. Mantido pela skill `memoria-sessao` (`.claude/skills/memoria-sessao/SKILL.md`).

Objetivo: qualquer nova conversa (ou pessoa) consegue ler as entradas mais recentes abaixo e entender rapidamente onde o trabalho parou, sem precisar reconstruir o contexto a partir do histórico de chat ou do git log.

Para o que foi implementado tecnicamente (não o que aconteceu em cada sessão), veja `docs/GERAL.md`.

---

## 2026-07-20 — Cobertura financeira, hardening de rate limit e infraestrutura de acesso

- **Autor/Interlocutor:** Walesson
- **O que foi feito:**
  - Estabelecida a arquitetura híbrida do MCP: módulos passthrough (`src/tools/*.ts`) para endpoints que a Omie já devolve prontos, e módulos em camadas (`src/modules/<nome>/`, clean architecture) quando há lógica de negócio (agregação, cruzamento, paginação).
  - Construídos módulos em camadas: `produtos` (estoque calculado por produto), `estoque` (total por produto somando locais), `ordemProducao` (OPs com produto resolvido), `pedidoVenda` (pedidos com cliente/itens, produtos para separar, atalho "Separar Estoque"), `clientesFornecedores` (cliente e fornecedor são o mesmo cadastro Omie, diferenciados por tag — renomeado de `clientes` a pedido do usuário), `contasCorrentes`, `fluxoCaixa` (relatório de fluxo de caixa que a Omie não tem pronto, com contas favoritas e opção de saldo real).
  - Hardening do `OmieClient`: throttle de 300ms entre chamadas, retry com espera baseada na mensagem de erro da Omie, `mapWithConcurrency` para limitar fan-out — corrigiu bugs reais de rate limit ("consumo indevido"/"consumo redundante") e a descoberta de que a Omie rejeita duas chamadas concorrentes do mesmo método.
  - Criado `src/httpServer.ts`: API REST local (só `127.0.0.1`, sem autenticação) reaproveitando a mesma lógica do MCP, com rotas de descoberta de schema (`/tools`, `/tools/:name/schema`) — passo intermediário rumo a um futuro frontend/backend próprio, sem expor o MCP remotamente ainda.
  - Adicionados os módulos `contasPagar` e `contasReceber` (listagem com nome do fornecedor/cliente resolvido), documentados em README.md e FUNCIONALIDADES.md, e implementado filtro de data (`data_alteracao_de`/`ate`) em ambos.
  - Criado `COMO_USAR.md` na raiz — guia de referência com todos os comandos `curl` e payloads de cada ferramenta, para consulta quando os tokens do Claude acabarem.
  - Promovidas as skills `documentacao-viva` e `memoria-sessao` de locais (`omie-mcp/.claude/skills/`) para globais (`~/.claude/skills/`), disponíveis em qualquer projeto novo.
- **Decisões tomadas:**
  - Escopo de clientes/fornecedores fica **só leitura** até o MCP ter segurança mínima implantada — CRUD completo fica para depois.
  - MCP não será exposto remotamente (Connector) até ter segurança mínima — a API HTTP local não muda essa decisão, só facilita consumo local.
  - O filtro nativo da Omie (`filtrar_por_data_de/ate`) em `financas/contapagar`/`financas/contareceber` filtra pela **data de última alteração do lançamento**, não pelo vencimento — confirmado testando a API. Por isso o MCP expõe o parâmetro como `data_alteracao_de/ate`, para não sugerir um comportamento que a API não tem.
- **Pendências / próximos passos:**
  - Gap-analysis pendente: mapear o que ainda falta cobrir da Omie (CRM, NF-e, Serviços/NFS-e, Painel do Contador nunca foram explorados; várias tools de finanças como extrato, boleto, pix, orçamentos ainda só via `omie_chamar_api` genérico).
  - Confirmar se as duas contas "iFood" cadastradas em `CONTAS_FAVORITAS` do fluxo de caixa são duplicidade real ou contas distintas legítimas.
  - Avaliar se remove as cópias locais das skills em `omie-mcp/.claude/skills/` agora que existem globais.
- **Docs relacionadas:** README.md (arquitetura, todos os módulos, rate limit), FUNCIONALIDADES.md, `COMO_USAR.md` (novo).

## 2026-07-20 — Segurança mínima na API HTTP local (autenticação por API key)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** implementada autenticação por API key estática no `httpServer.ts` — toda rota agora exige header `Authorization: Bearer <HTTP_API_KEY>`, validado com comparação timing-safe; o servidor recusa subir (`process.exit(1)`) se `HTTP_API_KEY` não estiver definida no `.env`. Criado `scripts/gerar-api-key.mjs` (`npm run gerar-api-key`) pra gerar a chave. Atualizados `.env.example`, `README.md` e `COMO_USAR.md` (todos os exemplos de `curl` agora incluem o header). Testado manualmente: 401 sem header/com chave errada, 200 com chave certa; servidor recusa iniciar sem `HTTP_API_KEY`.
  - **Atualizado em:** 2026-07-20 — adicionadas mais duas camadas: rate limit (janela fixa, 120 req/min, `429` acima disso) e confirmação obrigatória (`"confirmar": true` no payload) em ferramentas que incluem/alteram/excluem dado na Omie. Novo campo `destructive?: boolean` em `ToolDef` (`src/tools/types.ts`), marcado em `omie_op_incluir/alterar/excluir`, `omie_estoque_ajuste_incluir`, `omie_requisicao_compra_incluir`, `omie_pedido_compra_incluir`; `omie_chamar_api` detecta pelo prefixo do `call` (Incluir/Alterar/Excluir/Cancelar/Deletar). Testado manualmente: 400 sem confirmar, passa da checagem com confirmar, 429 a partir da 121ª requisição no minuto.
- **Decisões tomadas:** API key estática (não Basic Auth, não mTLS) é o mínimo suficiente pro estágio atual — uso local, single-user. Decisão tomada com o usuário via pergunta direta antes de implementar. Fica como base pra evoluir (ex: OAuth) se o servidor for exposto como Connector remoto no futuro — isso continua bloqueado por ora. Rate limit e confirmação foram escolhidos (entre as opções levantadas) como as camadas de maior retorno pro estágio atual — auditoria e chave por escopo (leitura/escrita) ficaram pra quando a escrita estiver em uso real.
- **Pendências / próximos passos:** com a autenticação mínima implantada, destravar os itens que dependiam dela: CRUD completo de clientes/fornecedores (hoje só leitura) e inclusão/consulta de contas a pagar/receber. MCP (stdio) em si não foi alterado — a lacuna de segurança era só na API HTTP.
- **Docs relacionadas:** [API](./API.md#autenticação-por-api-key-na-api-http-local-httpserverts), [API — rate limit e confirmação](./API.md#rate-limit-e-confirmação-obrigatória-em-operações-destrutivas-na-api-http-local), [Índice Geral](./GERAL.md)

## 2026-07-20 — Correção do gatilho da skill memoria-sessao

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** identificado que um chat novo desta mesma sessão não registrou o contexto de implementação porque a skill `memoria-sessao` só disparava "ao perceber que a conversa está encerrando" — sinal que pode nunca chegar (compactação, troca de chat, fechamento abrupto), como de fato não chegou. Reescrita a skill (`~/.claude/skills/memoria-sessao/SKILL.md`, versão global): o gatilho agora é por **entrega** — dispara logo após cada implementação/decisão relevante concluída, dentro da própria resposta, sem esperar o fim da conversa. Removida a cópia local duplicada em `omie-mcp/.claude/skills/` (documentacao-viva e memoria-sessao) — só resta a versão global, eliminando o risco de desalinhamento entre cópias que já era uma pendência registrada.
- **Decisões tomadas:** confirmação ao usuário antes de salvar só é pedida uma vez por sessão/dia (no primeiro registro); registros seguintes no mesmo dia atualizam a entrada existente direto, com só um aviso curto no fim da resposta — evita interromper o fluxo de trabalho a cada item, mas mantém transparência (nunca silencioso). Uma entrada por dia por tópico, atualizada incrementalmente, em vez de uma entrada por implementação.
- **Pendências / próximos passos:** ~~nenhuma~~ — ver atualização abaixo.
- **Docs relacionadas:** `~/.claude/skills/memoria-sessao/SKILL.md`.
- **Atualizado em 2026-07-20:** mesmo com o gatilho corrigido, o registro ainda depende do modelo lembrar sozinho — insuficiente (falhou de novo num chat que fez trabalho real: três módulos, `estoque`/`produtos`/`ordemProducao`, ganharam gateway abstraído por interface + fake gateway + teste unitário com `OMIE_MOCK`, e nada disso foi para `docs/CONTEXTO-SESSOES.md`). Criado hook determinístico via harness: `.claude/settings.json` (novo, local ao projeto) com `PostToolUse` em `Edit|Write`, rodando `.claude/hooks/memoria-sessao-reminder.mjs` — script Node que injeta um lembrete de sistema sempre que o arquivo editado está dentro de `src/**` (silencioso pra qualquer outro caminho). Testado: dispara em `Edit`/`Write` com paths relativos e absolutos, não dispara fora de `src/`. Isso não escreve a entrada sozinho (ainda depende do modelo agir sobre o lembrete), mas garante que o lembrete chega de forma determinística, não mais só por instrução de skill.
- **Pendência real:** ~~os três módulos citados (`estoque`, `produtos`, `ordemProducao` — padrão gateway+interface+fake+teste, já commitados) e o trabalho equivalente em `pedidoVenda`/`clientesFornecedores` (staged, não commitado) ainda não têm entrada própria em `docs/CONTEXTO-SESSOES.md` — registrar numa próxima resposta.~~ Registrado na entrada abaixo.

## 2026-07-20 — Abstração de gateways da Omie com interfaces + fake gateways + testes (vitest)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** para os 9 módulos em camadas (`estoque`, `produtos`, `ordemProducao`, `pedidoVenda`, `clientesFornecedores`, `contasCorrentes`, `fluxoCaixa`, `contasPagar`, `contasReceber`), extraída uma interface (`domain/interfaces/<nome>-gateway.ts`) de cada gateway concreto, com o gateway real passando a `implements` essa interface; criado um gateway fake em memória (`infrastructure/gateways/<nome>-fake-gateway.ts`) para cada um; use cases passaram a depender das interfaces (não das classes concretas), inclusive nas dependências cruzadas entre módulos (ex: `pedidoVenda`/`contasPagar`/`contasReceber` dependem de `IClientesGateway`, `fluxoCaixa` depende de `IContasCorrentesGateway`); tools em `presentation/mcp/*-tools.ts` escolhem gateway real ou fake via `process.env.OMIE_MOCK === "true"` (padrão: real, sem essa env var); `index.ts` de cada módulo passou a exportar a interface e as duas implementações. Instalado `vitest` (`npm run test`), com testes de use case usando os fakes — 21 testes no total, cobrindo soma de estoque, cruzamento produto+estoque, enriquecimento de OP, resolução de cliente/etapa em pedidos, filtro de cancelados, dedup do fake de clientes, agregação de fluxo de caixa (realizado vs. previsto, exclusão de cancelado, saldo real a partir de saldo_inicial/saldo_data), e resolução de fornecedor/cliente em contas a pagar/receber (incluindo o caso de código não encontrado no cadastro). `tsconfig.json` passou a excluir `**/*.test.ts` do build de produção (achado ao rodar `npm run build` depois do primeiro teste — sem isso o `.test.js` ia parar no `dist/`). Validado manualmente fim a fim via `httpServer.ts` com `OMIE_MOCK=true` em todos os 9 módulos.
- **Decisões tomadas:** piloto começou por `estoque` (módulo mais simples) pra validar o padrão antes de replicar; ordem dos módulos seguintes decidida um a um pelo usuário a cada resposta, não em lote. Quando um gateway concreto é reaproveitado por outro módulo (ex: `EstoqueOmieGateway` usado por `produtos`), o tipo de retorno (`ProdutoOmie`, `PosicaoEstoque` etc.) é re-exportado do arquivo do gateway concreto para não quebrar imports existentes entre módulos. `clientesFornecedores` e `contasCorrentes` ganharam interface/fake como pré-requisito de outros módulos (`pedidoVenda` e `fluxoCaixa`, respectivamente) antes de serem pedidos explicitamente — as tools desses dois módulos são passthrough/chamam `client` direto, então não precisaram de mudança, só o teste do fake. Registrada a convenção em `docs/PADRAO-PROJETO.md` agora que está completa nos 9 módulos.
- **Pendências / próximos passos:** nenhuma — os 9 módulos estão no padrão, build e suíte de testes (21/21) passando.
- **Docs relacionadas:** [Padrão de Projeto — gateway abstraído](./PADRAO-PROJETO.md#gateway-abstraído-por-interface--fake-gateway--omie_mock), [Índice Geral](./GERAL.md).

## 2026-07-20 — Módulo de estrutura de produtos (BOM/ficha técnica) com busca por nome

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** identificada a lacuna "o MCP não sabia buscar a estrutura de um produto pelo nome" (ex: "estrutura do produto 100kg" exigia já saber o código Omie). Investigado ao vivo contra a API real: `ConsultarEstrutura` estava com o resource errado (`produtos/malha`, correto é `geral/malha`); confirmado que o método `ListarEstruturas` (mesmo resource) existe e já devolve nome do produto e de cada insumo prontos (não precisa cruzar com o cadastro de produtos). Criado o módulo `src/modules/estrutura/` (padrão camadas: gateway+interface+fake+teste) com duas ferramentas: `omie_estrutura_listar` (todos os produtos com ficha técnica) e `omie_estrutura_buscar_por_produto` (acha pela descrição/trecho dela ou código, paginando client-side já que a API não tem busca por texto nesse endpoint). Removido o antigo `omie_estrutura_consultar` (passthrough com resource errado) do módulo `ordemProducao`. Validado fim a fim contra a conta Omie real (produto "100kg", código `9116171995`/`100kg`) e via `npm run build` + `npx vitest run` (26/26 passando).
- **Decisões tomadas:** a busca por nome pagina a listagem inteira (597 registros / 120 páginas nessa conta) em vez de tentar um filtro server-side, porque `ListarEstruturas` não aceita filtro de texto (só paginação) — se o volume crescer muito e isso incomodar, cachear é o próximo passo natural, não reescrever a busca.
- **Pendências / próximos passos:** ~~a ferramenta nova só fica disponível depois que a conexão MCP for reiniciada~~ — segue valendo pra todo o módulo depois da atualização abaixo.
- **Docs relacionadas:** README.md (seção "Estrutura de Produtos"), FUNCIONALIDADES.md.
- **Atualizado em 2026-07-20:** completado o CRUD do módulo, já seguindo o padrão gateway+interface+fake+teste desde o início (aprendido do ciclo de `produtos` logo acima). `IEstruturaGateway` ganhou `incluirItensEstrutura`/`alterarItensEstrutura`/`excluirItemEstrutura`; implementado em `EstruturaOmieGateway` e `EstruturaFakeGateway` (que também teve o bug do array `ESTRUTURAS_FAKE` compartilhado por referência corrigido, mesmo caso do `ProdutosFakeGateway`). Validado ao vivo com round-trip completo e seguro: criados 2 produtos de teste descartáveis (pai tipo '04 - Produto Acabado' + insumo), `IncluirEstrutura` → `AlterarEstrutura` → `ExcluirEstrutura` no item, depois os 2 produtos de teste excluídos — nenhum rastro ficou na conta. Achados reais que corrigem a doc pública da Omie: (1) o produto pai só aceita `IncluirEstrutura` se `tipoItem` for '03 - Produto em Processo' ou '04 - Produto Acabado'; (2) `intMalha` é obrigatório no item de `IncluirEstrutura`, apesar de listado como opcional; (3) `AlterarEstrutura` exige `idProdMalha` junto do `idMalha` mesmo só pra mudar quantidade. Corrigido também um bug preexistente nos dados fake (`ESTRUTURAS_FAKE`): os dois itens do produto 111 tinham `idMalha: 1` duplicado — pego pelo teste de exclusão, que falhava porque o `some()` ainda achava idMalha 1 depois de remover só o primeiro. Novos testes em `estrutura-crud.test.ts` (5 casos) — suíte total 37/37.

## 2026-07-20 — CRUD completo de Produtos (Incluir/Alterar/Excluir)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** pesquisado o catálogo completo de endpoints "produto" da Omie (developer.omie.com.br/service-list) e comparado com o que já existia no código — antes só havia Consultar/Listar. Primeira versão foi implementada como passthrough direto (`resource`+`call`, sem gateway); validada ao vivo com round-trip seguro (criar → alterar → excluir o mesmo produto de teste, sem deixar rastro) — achado real: `IncluirProduto` exige `codigo` (SKU) como obrigatório, embora a doc pública da Omie marque como opcional. **Refeito a pedido do usuário** pra seguir o padrão já consolidado do projeto (gateway abstraído por interface + fake gateway + teste, ver `docs/PADRAO-PROJETO.md`): `IProdutosGateway` ganhou `incluirProduto`/`alterarProduto`/`excluirProduto`; `ProdutosOmieGateway` implementa contra a Omie real; `ProdutosFakeGateway` passou a de fato criar/alterar/excluir em memória (e seu construtor foi corrigido pra não compartilhar mais o array `PRODUTOS_FAKE` por referência entre instâncias — antes era só leitura e isso não importava, agora mutação vazaria estado entre testes); criados use-cases finos (`IncluirProdutoUseCase`/`AlterarProdutoUseCase`/`ExcluirProdutoUseCase`) e DTOs Zod (`produto-crud.dto.ts`); as tools MCP passaram a chamar os use-cases em vez do passthrough. Adicionados 6 testes novos (`produto-crud.test.ts`) cobrindo incluir, duplicata recusada, alterar por código, alterar inexistente falha, excluir, e isolamento de estado entre instâncias do fake — tudo sem tocar na Omie real.
- **Decisões tomadas:** a validação ao vivo contra a API real (round-trip) foi mantida como o que descobriu o comportamento verdadeiro da API (campo `codigo` obrigatório) — o padrão gateway+fake resolve *testar depois*, não substitui a necessidade de validar contra a API real pelo menos uma vez ao implementar algo novo. Ficou de fora deste ciclo o restante do catálogo de "produto" mapeado na pesquisa anterior (características, kit, variação, lote, unidade de medida, tabela de preços, NCM, produto-fornecedor, comprador, locais/resumo de estoque) — implementar só o que o usuário pedir, não antecipar.
- **Pendências / próximos passos:** as ferramentas novas só ficam disponíveis depois que a conexão MCP for reiniciada (mesmo caso do módulo `estrutura` desta mesma sessão).
- **Docs relacionadas:** README.md (seção "Produtos"), FUNCIONALIDADES.md, [Padrão de Projeto — gateway abstraído](./PADRAO-PROJETO.md#gateway-abstraído-por-interface--fake-gateway--omie_mock).

## 2026-07-20 — CRUD de Ordem de Produção (OP) refeito seguindo o padrão do projeto

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** as ferramentas `omie_op_incluir`/`alterar`/`excluir`/`consultar` (já existentes, mas como passthrough puro) foram refeitas seguindo o padrão gateway+interface+fake+teste consolidado nos ciclos anteriores desta sessão (`produtos`, `estrutura`). `IOrdemProducaoGateway` ganhou `consultarOP`/`incluirOP`/`alterarOP`/`excluirOP`; implementado em `OpOmieGateway` (Omie real) e `OpFakeGateway` (memória — também corrigido o mesmo bug de array `ORDENS_FAKE` compartilhado por referência entre instâncias, mesmo caso de `produtos`/`estrutura`); use-cases finos + DTOs Zod (`op-crud.dto.ts`); tools MCP passaram a chamar os use-cases. Validado ao vivo com round-trip seguro e completo: criados 2 produtos de teste descartáveis (produto + insumo), estrutura ligando os dois, `IncluirOrdemProducao` → `ConsultarOrdemProducao` → `AlterarOrdemProducao` → `ExcluirOrdemProducao`, depois estrutura e os 2 produtos excluídos — nenhum rastro ficou na conta. Achados reais que corrigem a doc pública da Omie: (1) só é possível gerar OP de produto que já tenha estrutura (BOM) preenchida — erro `SOAP-ENV:Client-109` caso contrário; (2) `codigo_local_estoque` é obrigatório mesmo na inclusão simples (a doc marca como opcional), 0 = local padrão; (3) o status de exclusão bem-sucedida vem com `cCodStatus: "1"`, diferente de Incluir/Alterar/Consultar (`"0"`) — inconsistência da própria Omie, documentada no código. Adicionados 6 testes novos (`op-crud.test.ts`) — suíte total 43/43.
- **Decisões tomadas:** manter o padrão de sempre validar ao vivo contra a API real antes de codar o gateway definitivo (assim como em `produtos` e `estrutura`) — é o que descobre os campos obrigatórios reais, que às vezes divergem da doc pública da Omie.
- **Pendências / próximos passos:** as ferramentas atualizadas só refletem o novo comportamento depois que a conexão MCP for reiniciada (mesmo caso dos módulos anteriores desta sessão).
- **Docs relacionadas:** README.md (seção "Ordem de Produção").

## 2026-07-20 — CRUD de Clientes/Fornecedores (destravado pós-segurança mínima)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** implementado CRUD completo de clientes/fornecedores (`omie_clientes_incluir`/`alterar`/`excluir`), pendência registrada desde a sessão de segurança mínima ("CRUD completo de clientes/fornecedores... fica pra depois de reforçar a segurança do MCP" — já reforçada com API key + rate limit + confirmação obrigatória). Seguido o mesmo padrão gateway+interface+fake+teste dos ciclos anteriores (`produtos`, `estrutura`, `ordemProducao`): `IClientesGateway` ganhou `incluirCliente`/`alterarCliente`/`excluirCliente`; implementado em `ClientesOmieGateway` e `ClientesFakeGateway` (mesmo bug de array compartilhado por referência corrigido). Como o módulo ainda não tinha pastas `application/` (só tools passthrough + gateway), criadas `application/dto/cliente-crud.dto.ts` e `application/use-cases/` com 3 use-cases finos. Testes de CRUD adicionados no estilo já existente no módulo (direto contra o gateway, não via use-case isolado, mantendo o padrão do `clientes-fake-gateway.test.ts` preexistente) — 6 casos novos, suíte total 49/49. Validado ao vivo com round-trip seguro (criar→alterar→excluir um cliente de teste, sem deixar rastro): achado real que corrige a doc pública da Omie — `codigo_cliente_integracao` é obrigatório em `IncluirCliente`, mesmo listado como opcional.
- **Decisões tomadas:** cliente/fornecedor continua sendo o MESMO cadastro Omie (`geral/clientes`), diferenciado só pela tag — o Incluir aceita `tags: [{ tag: 'Cliente' }]` ou `[{ tag: 'Fornecedor' }]` pra decidir qual é.
- **Pendências / próximos passos:** as ferramentas novas só ficam disponíveis depois que a conexão MCP for reiniciada (mesmo caso dos módulos anteriores desta sessão).
- **Docs relacionadas:** README.md (seção "Clientes e Fornecedores"), FUNCIONALIDADES.md.

## 2026-07-20 — CRUD de Pedido de Venda

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** implementado CRUD de Pedido de Venda (`omie_pedido_venda_incluir`/`alterar`/`excluir`, e `omie_pedido_venda_consultar` migrado de passthrough pra use-case), módulo que antes só tinha listagem/relatórios. Seguido o mesmo padrão gateway+interface+fake+teste dos ciclos anteriores: `IPedidoVendaGateway` ganhou `consultarPedido`/`incluirPedido`/`alterarPedido`/`excluirPedido`; implementado em `PedidoVendaOmieGateway` e `PedidoVendaFakeGateway` (mesmo bug de array compartilhado por referência corrigido); criadas `application/dto/pedido-crud.dto.ts` e 4 use-cases finos; 6 testes novos (`pedido-crud.test.ts`) — suíte total 55/55. Validado ao vivo com round-trip completo e seguro: criados cliente + produto de teste descartáveis, descoberto que o cliente precisava de UF preenchida (erro `SOAP-ENV:Client-1035` senão) e que era preciso achar um `codigo_categoria` de receita (`1.01.01`, via `ListarCategorias`) e um `codigo_conta_corrente` real (`ListarContasCorrentes`) — a Omie recusa pedido sem esses dois campos, mesmo em venda simples. `IncluirPedido` → `ConsultarPedido` → `AlterarPedidoVenda` → `ExcluirPedido`, depois cliente e produto excluídos — nenhum rastro ficou na conta.
- **Decisões tomadas:** os itens enviados em `AlterarPedidoVenda` substituem os itens atuais do pedido (não fazem merge) — documentado na descrição da tool pra evitar surpresa. `etapa` (padrão `'10'`) e `codigo_parcela` (padrão `'000'` = à vista) ficaram com default pra reduzir campos obrigatórios no caso comum.
- **Pendências / próximos passos:** as ferramentas novas só ficam disponíveis depois que a conexão MCP for reiniciada (mesmo caso dos módulos anteriores desta sessão). Os métodos extras do recurso `produtos/pedido` descobertos na pesquisa (`AlterarPedFaturado`, `DevolverPedido`, `StatusPedido`, `TrocarEtapaPedido`, `SimularImpostos`) ficaram de fora deste ciclo — implementar só quando o usuário precisar.
- **Docs relacionadas:** README.md (seção "Pedido de Venda"), FUNCIONALIDADES.md.

## 2026-07-20 — CRUD de Ajuste de Estoque

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** implementado `omie_estoque_ajuste_excluir` (faltava — só existia Incluir) e migrado `omie_estoque_ajuste_incluir` de passthrough pra use-case, seguindo o padrão gateway+interface+fake+teste. `IEstoqueGateway` ganhou `incluirAjuste`/`excluirAjuste`; implementado em `EstoqueOmieGateway` e `EstoqueFakeGateway` (mesmo bug de array compartilhado por referência corrigido, e passou a rastrear ajustes ativos num Map pra suportar exclusão). Criadas `application/dto/ajuste-estoque.dto.ts` e 2 use-cases; 6 testes novos (`ajuste-estoque.test.ts`) — suíte total 61/61. Validado ao vivo: descoberto que o campo `motivo` só aceita 4 valores (`INI`/`INV`/`OPE`/`PDV`) via mensagem de erro de validação da Omie (não documentado na doc pública), e nomes de campo reais diferentes do que a doc pública sugeria (`id_prod`, `data`, `tipo`, `quan`, `valor`, `obs`, `origem`, `motivo` — snake_case simples, sem prefixo).
- **Decisões tomadas:** **achado importante, sem solução**: depois de incluir QUALQUER ajuste de estoque num produto, esse produto nunca mais pode ser excluído (`ExcluirProduto` recusa com "Movimentos de Estoque (calculados)"), mesmo excluindo o ajuste em seguida — é uma trava permanente da própria Omie, documentada na descrição das duas tools e no README pra avisar o usuário antes de ajustar estoque de produto de teste/temporário.
- **Pendências / próximos passos:** as ferramentas atualizadas só refletem o novo comportamento depois que a conexão MCP for reiniciada (mesmo caso dos módulos anteriores desta sessão). **Ficou um produto de teste residual na conta real** (`codigo_produto` 9568967822, código "TESTE-MCP-EST", renomeado para "TESTE MCP ESTOQUE (residual - não usar)" e marcado inativo) — não foi possível excluí-lo por causa da trava acima; é inofensivo (inativo, claramente identificado) mas fica registrado aqui pra não confundir ninguém que encontrar esse cadastro depois. `ListarAjusteEstoque` (listagem de ajustes) não foi implementado neste ciclo — o teste retornou erro de schema (`NPAGINA` não reconhecido) e não valia a pena investigar mais a fundo fora do escopo pedido (CRUD).
- **Docs relacionadas:** README.md (seção "Estoque"), FUNCIONALIDADES.md.

## 2026-07-20 — Investigação de CRUD em Contas Correntes (pausado)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** ao tentar implementar CRUD de contas correntes (`geral/contacorrente`) seguindo o mesmo padrão dos outros módulos, o teste ao vivo revelou um comportamento anômalo: `IncluirContaCorrente` devolve sucesso (`cCodStatus: "0"`, com `nCodCC` gerado) — testado 2 vezes, sempre "sucesso" — mas `ConsultarContaCorrente`, `AlterarContaCorrente` e `ExcluirContaCorrente` com esse mesmo `nCodCC` dizem que a conta NÃO existe (`cCodStatus: "100"`, "Conta Corrente não cadastrada"). Esperado 15s (tempo bem maior que qualquer delay de propagação visto em outros endpoints nesta sessão — produtos, estrutura, OP, clientes, pedido todos resolveram em segundos) e o problema persistiu. `ListarContasCorrentes` também não lista a conta criada (total_de_registros continua 49, igual antes do Incluir).
- **Decisões tomadas:** apresentado o achado ao usuário com 3 opções (implementar mesmo assim documentando o risco / investigar mais / pular por agora) — usuário escolheu **pular por agora**. CRUD de contas correntes NÃO foi implementado; o módulo continua só leitura (`omie_contas_correntes_listar`).
- **Pendências / próximos passos:** investigar esse comportamento quando houver tempo/interesse — hipóteses não descartadas: (1) delay de indexação real mas bem mais longo que o normal nesta conta específica; (2) `IncluirContaCorrente` sendo uma operação sem efeito real nesse plano Omie (alguma restrição não sinalizada por erro); (3) bug real da API Omie. Não foi possível confirmar qual.
- **Docs relacionadas:** nenhuma (módulo não alterado).

## 2026-07-20 — Filtro genérico replicado pros demais módulos com listagem enriquecida

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** replicado o parâmetro `filtros` (piloto em `omie_op_listar_com_produto`) pros demais use-cases de listagem enriquecida: `omie_pedido_venda_listar_com_cliente`, `omie_pedido_venda_separar_estoque_listar` (repassa pro use-case interno), `omie_pedido_venda_produtos_para_separar` (filtra os itens antes de agregar o resumo por produto), `omie_produtos_listar_com_estoque` e `omie_estrutura_listar`. Cada DTO ganhou o campo `filtros: filtrosParamSchema` e cada use-case passou a chamar `aplicarFiltros(itens, param.filtros)` depois dos filtros nativos/já existentes. 5 testes novos (um por use-case tocado) — suíte total 75/75. Descrições das tools atualizadas com exemplos de campo pra cada uma. README.md ganhou uma nota geral explicando o parâmetro `filtros` (operadores, dot-path, AND, quando preferir filtro nativo).
- **Decisões tomadas:** `omie_estrutura_buscar_por_produto` NÃO ganhou `filtros` — o parâmetro `termo` já cobre busca por descrição/código, adicionar filtro genérico ali seria redundante. Em `listar-produtos-para-separar`, o filtro é aplicado nos itens (nível pedido×produto) ANTES de montar o resumo agregado por produto, pra que o resumo reflita o filtro.
- **Pendências / próximos passos:** nenhuma — rollout do filtro genérico está completo em todos os módulos com listagem enriquecida identificados.
- **Docs relacionadas:** [Padrão de Projeto — filtro genérico](./PADRAO-PROJETO.md#filtro-genérico-client-side-sharedfiltrots), README.md (nota geral no topo de "Ferramentas disponíveis").

## 2026-07-20 — Gap-analysis completo do catálogo Omie (pendências para próximas sessões)

- **Autor/Interlocutor:** Walesson
- **O que foi feito:** pesquisado o catálogo completo de serviços da Omie (developer.omie.com.br/service-list) e comparado com o que o MCP já cobre. Resultado: módulos cobertos hoje (mesmo que parcialmente) — Produtos, Estrutura, Famílias, Clientes/Fornecedores, Contas Correntes (só listar), Estoque (ajuste/movimento/consulta), Requisição/Pedido de Compra, Ordem de Produção, Pedido de Venda + Etapas de Faturamento, Contas a Pagar/Receber, Movimentos Financeiros (fluxo de caixa calculado na unha). Categorias inteiras SEM nenhuma ferramenta dedicada (só acessíveis hoje via `omie_chamar_api` genérico): **CRM** (Contas, Contatos, Oportunidades, Tarefas, Fases — funil de vendas B2B, se usado dentro da Omie); **Serviços/NFS-e** (Serviço, Ordem de Serviço, Contrato de Serviço recorrente, NFS-e); **NF-e/Fiscal** (emitir/consultar NF-e, Cupom Fiscal/NFC-e, CT-e, Remessa — hoje o MCP não emite nenhum documento fiscal); **Painel do Contador** (XMLs fiscais, resumo de fechamento); **Financeiro avançado** (Boletos, PIX, Extrato de Conta Corrente, Lançamentos de conta corrente, Orçamento de Caixa nativo da Omie — nunca testado, pode substituir o cálculo manual do módulo `fluxoCaixa`); **Compras avançado** (Nota de Entrada + faturamento, Recebimento de NF-e); **Cadastros auxiliares** (Bancos, Unidades, Categorias, Cidades, Países, NCM, Tabela de Preços, Cenário de Impostos, Características de produto, Kit, Variação, Lote de produto).
- **Decisões tomadas:** nenhuma implementação ainda — usuário pediu só o levantamento, e pediu pra documentar aqui pra outra sessão/chat continuar a partir disso.
- **Pendências / próximos passos (ordem de prioridade sugerida, a confirmar com o usuário antes de começar):**
  1. **NF-e/Cupom Fiscal** — hoje é lacuna total; vira essencial se o MCP precisar emitir nota em algum momento, não só consultar dados.
  2. **Serviços/NFS-e** — só relevante se a empresa presta serviço (ex: industrialização por encomenda), não só vende produto.
  3. **CRM** — só relevante se o funil de vendas B2B roda dentro da Omie (separado do pedido de venda comum já coberto).
  4. **Financeiro avançado** (Extrato de Conta Corrente, Boletos, PIX, Orçamento de Caixa nativo) — complementa/pode simplificar o módulo `fluxoCaixa` existente.
  5. Cadastros auxiliares (Bancos, Unidades, NCM, etc.) e Compras avançado (Nota de Entrada) — baixa prioridade, cadastros estáticos ou fluxo pouco usado.
  - Antes de implementar qualquer item: seguir o padrão já consolidado do projeto — validar campos reais contra a API ao vivo com round-trip seguro (criar → testar → excluir, sem poluir dados reais), depois codar seguindo gateway+interface+fake+teste (`docs/PADRAO-PROJETO.md`).
- **Docs relacionadas:** nenhuma nova (só este registro).
- **Atualizado em 2026-07-20 — item 1 (NF-e) iniciado e concluído (parcialmente, por decisão do usuário):** pesquisado o catálogo de endpoints de NF-e da Omie (agente em background) — achado importante: **não existe endpoint de emissão de NF-e do zero** (tipo `IncluirNFe(itens, cliente)`) na doc oficial; a API trata NF-e majoritariamente como consulta/importação de documento já processado pelo motor fiscal do ERP. Como nota fiscal emitida é documento com efeito legal (sem round-trip seguro de criar→testar→excluir, diferente dos demais módulos), apresentadas 3 opções ao usuário — escolhida **"só consulta por enquanto"**. Implementado `src/modules/nfe/` (padrão gateway+interface+fake+teste): `omie_nfe_listar` (`ListarNF`, recurso `produtos/nfconsultar`, paginação, filtro por data/status/tipo + filtro genérico) e `omie_nfe_consultar` (`ConsultarNF` por chave de acesso ou código interno, com itens e títulos financeiros gerados pela nota). Validado ao vivo contra a conta real (4765 notas, testado inclusive caso de nota cancelada) via script descartável chamando o build direto — apagado depois. Suíte de testes: 75 → 82 (7 novos em `nfe.test.ts`). Emissão/cancelamento de NF-e continua pendente de investigação futura (endpoint real com suporte Omie, certificado digital, ambiente de homologação).
- **Docs relacionadas (NF-e):** [API — módulo NF-e](./API.md#módulo-nf-e-somente-leitura-produtosnfconsultar-omie_nfe_listar-listarnf-paginação-filtro-por-datastatustipo--filtro-genérico-e-omie_nfe_consultar-consultarnf-por-chave-ou-código-com-itens-e-títulos-financeiros), README.md (seção "Notas Fiscais / NF-e"), FUNCIONALIDADES.md (seção "Notas Fiscais (NF-e)").
- **Atualizado em 2026-07-20 — CRUD completo de Compras:** usuário pediu pra continuar com "compras" — módulo só tinha `IncluirRequisicaoCompra`/`IncluirPedidoCompra` como passthrough (sem Consultar/Alterar/Excluir/Listar). Escolhida a opção "CRUD completo" (recomendada) pro usuário. Pesquisada a doc oficial (`produtos/pedidocompra`, `produtos/requisicaocompra`) e validado tudo ao vivo com round-trip completo e seguro (fornecedor + produto de teste descartáveis, criados e excluídos sem deixar rastro): `IncluirPedCompra`→`ConsultarPedCompra`→`PesquisarPedCompra`→`AlteraPedCompra`→`ExcluirPedCompra` e o mesmo ciclo pra `IncluirReq`→`ConsultarReq`→`PesquisarReq`→`AlterarReq`→`ExcluirReq`. Criado `src/modules/compras/` (padrão gateway+interface+fake+teste), removido `src/tools/compras.ts` antigo. Achados reais importantes: (1) `nCodCC` do Pedido de Compra exige código de CONTA CORRENTE, não de departamento/centro de custo (nome enganoso, doc pública não deixa claro); (2) `PesquisarPedCompra` esconde todos os pedidos por padrão — resolvido sempre enviando todas as flags `lExibirPedidos*` como `'S'` no gateway; página sem registros vira erro (`SOAP-ENV:Client-5113`) em vez de lista vazia, normalizado no gateway; (3) `IncluirReq`/`AlterarReq` da Requisição de Compra recebem os campos direto na raiz do `param`, sem o wrapper `requisicaoCadastro` que a doc pública sugere. Validado também via as próprias tools MCP ponta a ponta (não só chamada crua à Omie). Suíte de testes: 82 → 86 (4 novos testes). Documentação atualizada: README.md, FUNCIONALIDADES.md, docs/API.md, docs/GERAL.md.
- **Atualizado em 2026-07-20 — Extrato de conta corrente:** usuário pediu pra escolher o próximo item sozinho — escolhido "Financeiro avançado" (mais conectado ao que já existe: `contasCorrentes`/`fluxoCaixa`). Usuário avisou que ia dormir e pediu pra seguir o restante sozinho. Implementado `omie_extrato_conta_corrente_consultar` (`ListarExtrato`, recurso `financas/extrato`) como nova capability do gateway já existente de `contasCorrentes` (não um módulo novo) — movimentos e saldos (anterior/atual/conciliado/disponível) de uma conta num período, com filtro genérico. Validado ao vivo contra a conta real. Suíte: 86 → 89. Commitado separadamente (regra nova: um commit por módulo concluído, ver `feedback_commit_por_modulo` na memória).
- **Atualizado em 2026-07-20 — Boleto de contas a receber:** segundo item do "Financeiro avançado", trabalhando sozinho enquanto o usuário dormia. Implementado `omie_contas_receber_boleto_gerar/obter/prorrogar/cancelar` como nova capability do gateway já existente de `contasReceber` (`financas/contareceberboleto`). **Achado ao vivo importante**: esta conta Omie não tem convênio bancário configurado — `ProrrogarBoleto` testado num título real (sem gerar boleto) recusou com "Não temos suporte para geração da remessa de pagamento para o banco -sem instituição-". Por prudência, `GerarBoleto` não foi testado contra título de produção (documento financeiro real, sem round-trip seguro garantido) — só `ObterBoleto`/`CancelarBoleto`/`ProrrogarBoleto` validados ao vivo (respostas seguras de "nenhum boleto gerado"). Suíte: 89 → 92. Commitado separadamente.
- **Pendências / próximos passos:** restam do "Financeiro avançado": PIX (`financas/pix`), Orçamento de Caixa nativo (`financas/caixa`) — ainda não pesquisados/validados. Depois: Serviços/NFS-e e CRM (nenhum iniciado).
